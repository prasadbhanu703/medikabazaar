import styled from "styled-components";
import { theme } from "../../styles/theme";

export const ReviewContainer = styled.div`
    background: ${theme.reviewsBackground};
    border-radius: 8px;
    padding: 10px;
`